const routes = {
    home: '/',
    login: '/login',
    register: '/register',
    code_problem: '/code-problem',
    cv_matching: '/cv-matching',
    forbidden: '/forbidden',
}

export default routes;
